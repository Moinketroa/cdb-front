import { DiscontinuedDatePipe } from './discontinued-date.pipe';

describe('DiscontinuedDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DiscontinuedDatePipe();
    expect(pipe).toBeTruthy();
  });
});
