import { IntroducedDatePipe } from './introduced-date.pipe';

describe('IntroducedDatePipe', () => {
  it('create an instance', () => {
    const pipe = new IntroducedDatePipe();
    expect(pipe).toBeTruthy();
  });
});
